---
import { hubFaqs } from '../../../data/site';

export function getStaticPaths() {
	return hubFaqs.map((faq) => ({
		params: { slug: faq.slug },
		props: { slug: faq.slug },
	}));
}

const { slug } = Astro.props;
return Astro.redirect(`/faq/#${slug}`, 301);
---
