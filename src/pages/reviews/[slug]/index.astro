---
import { customerReviews } from '../../../data/site';

export function getStaticPaths() {
	return customerReviews.map((review) => ({
		params: { slug: review.slug },
	}));
}

return Astro.redirect('/reviews/', 301);
---
